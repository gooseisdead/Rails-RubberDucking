<h1>Wanna New Duck?</h1>

<%= form_for @duck do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :description %>
<%= f.text_field :description %>
<%= f.label :sudent_id %>
<%= f.collection_select :student_id, Student.all, :id, :name %>
<%= f.submit "Create New Duck" %>
<% end %>